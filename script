let products = JSON.parse(localStorage.getItem("products")) || [];

function addProduct(){
  let name = document.getElementById("name").value;
  let price = document.getElementById("price").value;

  let product = {
    name: name,
    price: price
  };

  products.push(product);
  localStorage.setItem("products", JSON.stringify(products));

  showProducts();
}

function showProducts(){
  let list = document.getElementById("productList");
  list.innerHTML = "";

  products.forEach((p, index) => {
    list.innerHTML += `
      <div style="border:1px solid gray; padding:10px; margin:5px;">
        <h3>${p.name}</h3>
        <p>৳ ${p.price}</p>
        <button onclick="buy()">কিনুন</button>
      </div>
    `;
  });
}

function buy(){
  alert("পণ্য কার্টে যোগ হয়েছে 👍");
}

// page load হলে দেখাবে
showProducts();
let products = JSON.parse(localStorage.getItem("products")) || [];
let cart = JSON.parse(localStorage.getItem("cart")) || [];

function addToCart(name, price){
  cart.push({name, price});
  localStorage.setItem("cart", JSON.stringify(cart));
  showCart();
}

function showCart(){
  let cartList = document.getElementById("cartList");
  let total = 0;

  cartList.innerHTML = "";

  cart.forEach((item, index) => {
    total += Number(item.price);

    cartList.innerHTML += `
      <div style="border:1px solid gray; padding:10px; margin:5px;">
        <h4>${item.name}</h4>
        <p>৳ ${item.price}</p>
      </div>
    `;
  });

  document.getElementById("totalPrice").innerText = "Total: ৳" + total;
}

function showProducts(){
  let list = document.getElementById("productList");
  list.innerHTML = "";

  products.forEach((p) => {
    list.innerHTML += `
      <div style="border:1px solid gray; padding:10px; margin:5px;">
        <h3>${p.name}</h3>
        <p>৳ ${p.price}</p>
        <button onclick="addToCart('${p.name}', '${p.price}')">কিনুন</button>
      </div>
    `;
  });
}

// load
showProducts();
showCart();
let cart = JSON.parse(localStorage.getItem("cart")) || [];

function addToCart(name, price){
  cart.push({name, price: Number(price)});
  localStorage.setItem("cart", JSON.stringify(cart));
  showCart();
}

function removeItem(index){
  cart.splice(index, 1);
  localStorage.setItem("cart", JSON.stringify(cart));
  showCart();
}

function showCart(){
  let cartList = document.getElementById("cartList");
  let total = 0;

  cartList.innerHTML = "";

  cart.forEach((item, index) => {
    total += item.price;

    cartList.innerHTML += `
      <div style="border:1px solid gray; padding:10px; margin:5px;">
        <h4>${item.name}</h4>
        <p>৳ ${item.price}</p>
        <button onclick="removeItem(${index})">❌ Remove</button>
      </div>
    `;
  });

  document.getElementById("totalPrice").innerText = "Total: ৳" + total;
}
<script src="https://www.gstatic.com/firebasejs/10.12.0/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore-compat.js"></script>
<script type="module">
  import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
  import { getFirestore, collection, addDoc } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
  import { getAuth } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";

  const firebaseConfig = {
    apiKey: "আপনার apiKey",
    authDomain: "digibazar-48743.firebaseapp.com",
    projectId: "digibazar-48743",
    storageBucket: "digibazar-48743.appspot.com",
    messagingSenderId: "295224613021",
    appId: "আপনার appId"
  };

  const app = initializeApp(firebaseConfig);
  const db = getFirestore(app);
  const auth = getAuth(app);

  // 🟢 Seller product add
  window.addSellerProduct = async function() {
    const name = document.getElementById("pname").value;
    const price = document.getElementById("pprice").value;

    const user = auth.currentUser;

    if (!user) {
      document.getElementById("msg").innerText = "আগে Login করুন ❌";
      return;
    }

    await addDoc(collection(db, "products"), {
      name: name,
      price: price,
      owner: user.email
    });

    document.getElementById("msg").innerText = "Product পোস্ট হয়েছে ✔";
  }
</script>
function showShop(){
  document.getElementById("loginPage").style.display = "none";
  document.getElementById("shopPage").style.display = "block";
}